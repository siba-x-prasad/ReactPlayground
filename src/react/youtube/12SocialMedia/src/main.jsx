import React from 'react'
import ReactDOM from 'react-dom/client'
import SocialMediaApp from './SocialMediaApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SocialMediaApp />
  </React.StrictMode>,
)
